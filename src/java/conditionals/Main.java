public class Main {
  public static void main(String[] args) {
    if (true) {
      // do
    } else if (false) {
      // do
    } else {
      // do
    }

    int i = true ? 1 : 0;
    switch (i) {
      case 0:
        // code block
        break;
      case 1:
        // code block
        break;
      default:
        // code block
    }
  }
}
